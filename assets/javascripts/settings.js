// Generated by CoffeeScript 1.6.3
(function() {
  var OccupancyPluginSettings;

  OccupancyPluginSettings = (function() {
    function OccupancyPluginSettings() {
      var _this = this;
      $('#new-occupancy-user').click(function(event) {
        event.preventDefault();
        return _this.new_user(event.target);
      });
      $(document).on('click', '.remove-occupancy-user', function(event) {
        event.preventDefault();
        $(event.target).parents('tr').first().remove();
        return _this.update_json_setting();
      });
      $(document).on('change', '.occupancy-users select', function() {
        return _this.update_json_setting();
      });
      true;
    }

    OccupancyPluginSettings.prototype.new_user = function(link) {
      var template;
      template = $($(link).data('template'));
      $(link).prev().find('tbody').append(template);
      return true;
    };

    OccupancyPluginSettings.prototype.update_json_setting = function() {
      var manager_users,
        _this = this;
      manager_users = {};
      $('.occupancy-users tr').each(function(idx, el) {
        var manager, users;
        manager = $('select', el).first().val();
        users = $('select', el).last().val();
        return manager_users[manager] = users;
      });
      return $('#settings_users').val(JSON.stringify(manager_users));
    };

    return OccupancyPluginSettings;

  })();

  jQuery(function() {
    return window.occupancyPluginSettings = new OccupancyPluginSettings();
  });

}).call(this);
